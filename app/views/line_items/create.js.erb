// hide alert msgs from before, if any.
$("div.alert").hide()

if ($('.cart tr').length == 1) { $('.cart').slideDown(); }

$("div.cart").html("<%= escape_javascript(render @cart) %>");
$("div.cart tr.current").css({'background-color':'#f0ad4e'}).animate({'background-color':'transparent'}, 1000);

